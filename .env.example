# Environment Variables Configuration
# This file serves as a template for environment variables.
# Copy this file to .env and replace placeholder values with your actual credentials.
# IMPORTANT: Never commit your actual .env file to version control!

# GitHub Personal Access Token
# Required for GitHub integrations and API access
# To create a token:
# 1. Go to GitHub Settings > Developer settings > Personal access tokens > Tokens (classic)
# 2. Generate a new token with appropriate permissions (repo, read:org, etc.)
# 3. Copy the token and replace the placeholder below
GITHUB_PAT=your_personal_access_token_here
